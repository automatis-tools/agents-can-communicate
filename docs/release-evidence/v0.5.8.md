# ACC 0.5.8 candidate evidence

Captured on 2026-09-13 with Node 26.5.1 and npm 11.17.0 on darwin-arm64.

| Item | Value |
|---|---|
| Clean source commit | `7fb2a3eb17098d9f0f70a0262e885fb88013a92d` |
| Artifact | `agents-can-communicate-0.5.8.tgz` |
| Size / files | 395,085 bytes / 278 files |
| SHA-256 | `0931a9376ea3b22f7e5da3cc48a05c0f1dd17abf64306b1066e73f3f5e4da732` |
| SHA-1 | `3101e2627cead2093373768b4e7a92c388ba883f` |
| npm integrity | `sha512-08q4lLjgTpcHgls0sMsheo4xMuBsfyFJmJQE9+FwHpt/VGIIjD2o4iZzt+39IMZX/0nrs1IGQUpT5tfp0+xcsQ==` |

Candidate A contains the approved concise README, its diagram and documentation
guard, plus version metadata. It was clean immediately before packing. Candidate B
contains only this file and CHANGELOG, which npm excludes. Verification used the
saved archive by filename.

## Installed artifact and upgrade

The package verifier passed against that exact archive: bundled packages,
certification evidence, documentation links, doctor, a workspace without Git, and
client install/uninstall restoration. Syntax validation covered 545 tracked files.
All 28 managed archive tests passed with the four-file concurrency limit.

A separate preflight installed the published 0.5.7 archive into private home, data
and project directories. Its SHA-256 was
`a515bb01e76a2912a16ddc89358dc1d60c149e7c8c1c0186cf837a83e91bfb18`.
It recorded actionable-delivery decisions for Claude and Codex, installed this exact
candidate, and ran `acc update`. The active runtime became 0.5.8 with phase `ready`;
both decisions remained unchanged. All 278 source, archive and installed files
matched byte for byte. The fixture used client shims and started no vendor service.
Its private installation was removed after the check.

The first preflight reached the correct version and preserved both decisions, then
its comparison script treated npm pack's JSON array as an object. Correcting that
local script's parser produced the complete passing comparison. No product code or
test was changed to obtain the result.

## Scope and inherited evidence

Compared with the published 0.5.7 archive, 261 files are byte-identical, 16 JSON
manifests change only their version, and README is the sole other changed file.
Runtime implementation and bundled capability fixtures are unchanged. Their
original capture versions, artifacts and limits still apply; this is not a new
native-client capture. In particular, 0.5.7's cold-start limitation remains.
See [the preceding release evidence](v0.5.7.md).

The diagram was rendered locally with light and dark themes, HTML and SVG labels,
and a simulated fallback to black text after the maintainer reported poor contrast.
The existing ordinary-task prompt guard now checks Getting Started. A deliberately
added ACC instruction made it fail; the original example was restored unchanged.

The full suite runs after candidate B through the unchanged pre-push hook. Merge
also requires the repository's five GitHub checks, including Node 24 on Linux and
macOS and installation of the packed artifact on both platforms.

## Retained evidence

Files are retained under `/private/tmp/acc-058-readme-release` and copied into the
local, Git-excluded media handover archive before publication.

| File | SHA-256 |
|---|---|
| `upgrade.json` | `c4b46d58accf8643bf47b2a55948ee119a10890eaf2abfdf210c9b037cd4db3e` |
| `published-code-comparison.json` | `451ca8255b0a66047c7f1d7178ec1a61285359ce1033810bc8008f9ebdb5d626` |
| `managed-packed.log` | `5f707f9528507a23bc00f0b68738f4cd61af353f1f74b99a9ae3725b42308138` |
| `verify.log` | `74b7a04aa38e26b27447616cf1bf3b9f03eb6c3c9ee0542470990377cca0a9b2` |

The earlier pack made before candidate A was committed is retained as
`uncommitted-attempt.tgz`; it is not the release artifact. Only the clean-source
archive identified above is eligible for publication.
