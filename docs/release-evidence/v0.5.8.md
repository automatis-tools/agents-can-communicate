# ACC 0.5.8 candidate evidence

Captured on 2026-09-13 with Node 26.5.1 and npm 11.17.0 on darwin-arm64.

| Item | Value |
|---|---|
| Clean source commit | `760bfc04492588c51843ed9bff92805c6ed8223e` |
| Artifact | `agents-can-communicate-0.5.8.tgz` |
| Size / files | 396,374 bytes / 279 files |
| SHA-256 | `9c144aa0cb2251bec68cd24e7aa7e1a65b7cadbeee798ac99cd21e9ad9e01456` |
| SHA-1 | `113ae2cfa153f55dc7c200ccaec6d28df80acf3b` |
| npm integrity | `sha512-3Cxg5p2kIrR3tEGuH5LQr8h4vQ6wiOQ5bMraZOdv6oC8erSGfamfA0LBLY5J7s07gXcn8e12Sv9TgRwf9gs/ig==` |

Candidate A contains the approved concise README, its diagram and documentation
check, version metadata, and Claude Channel endpoint-ready activation. It was clean
immediately before packing. Candidate B changes only this file and CHANGELOG,
which npm excludes. Verification used the saved archive by filename.

## Installed artifact and upgrade

The package verifier passed against that exact archive: bundled packages,
certification evidence, documentation links, doctor, a workspace without Git, and
client install/uninstall restoration. Syntax validation covered 548 tracked files.
All 37 combined managed-update and startup checks passed with the four-file
concurrency limit: 28 managed archive tests, one installed cold-start/MCP-reply
regression, and eight process cases covering lifecycle, ownership and consent.

A separate preflight installed the published 0.5.7 archive into private home, data
and project directories. Its SHA-256 was
`a515bb01e76a2912a16ddc89358dc1d60c149e7c8c1c0186cf837a83e91bfb18`.
It recorded actionable-delivery decisions for Claude and Codex, installed this exact
candidate, and ran `acc update`. The active runtime became 0.5.8 with phase `ready`;
both decisions remained unchanged. All 279 source, archive and installed files
matched byte for byte. The fixture used client shims and started no vendor service.
Its private installation was removed after the check.

Compared with published 0.5.7, the archive has 258 identical files, 15 version-only
manifests, five other changed files and one new entrypoint helper. The other changes
are README, ARCHITECTURE, the Channel entrypoint, native-attempt diagnostics and the
hook-runner exports. No file was removed.

## Native startup evidence and limits

The real-client check used Claude Code 2.1.270 on macOS arm64 and an unpublished
0.5.7 development archive, SHA-256
`8526ced0fcda0e4469d96f9c7304aeaca4b42caa283eddeca4bcfd3c8e75178b`.
It was built from base `43cc58bb07026648f292be895949b7c240558be2` with the startup
implementation later committed as `9e92cd4`, before the final architecture prose.
This is its original capture identity, not a new native run of the 0.5.8 archive.

The user submitted no initial prompt. SessionStart ran at 16:54:58.397Z; the Channel
registered after its endpoint became ready. A question sent at 16:56:02.222Z reached
the native Channel, which started the Claude turn and returned an `acc_reply`
answer. The return reached the busy Codex transport at 16:56:23.150Z. At the initial
observation point an automatic Codex model turn had not yet been seen; the committed
COMPATIBILITY record preserves that limit. The retained follow-up at 17:12:41.921Z
records the answer appearing automatically in that Codex session without an inbox
body retrieval. No raw client transcript was retained. Process absence was checked
separately after shutdown; SessionEnd itself was not captured.

A separate archive comparison checked all 279 captured files against that native
installation. Against the new candidate, 261 files are identical, 16 manifests
change only their version, and only README and ARCHITECTURE have other changes.
Every production runtime code file is identical to the cold-start-tested
installation. The independent combined build also produced the exact candidate
SHA-256 above and passed 13 startup/native/reply tests and its package verifier.
See [Claude compatibility](../../packages/adapter-claude-code/COMPATIBILITY.md) for
the dated capture and existing client/platform limits.

Removing endpoint-ready registration reproduces queued/unavailable delivery.
Separate mutations fail when the lifecycle lock or installation-consent check is
removed. The interrupted-close regression fails without journal recovery. Their
failing logs were inspected in independent review; fresh checks pass with the
protections present. No broader client or platform capability is certified here.

## README and final gates

The diagram was rendered with light and dark themes, HTML and SVG labels, and a
black-text fallback. The full diagram was also inspected in GitHub's expanded
Mermaid view. The ordinary-task prompt check now uses the existing Getting Started
example. Adding an ACC instruction made the check fail; the original example was
restored unchanged.

The full suite runs after candidate B through the unchanged pre-push hook. Merge
requires the repository's five GitHub checks, including Node 24 on Linux and macOS
and installation of the packed artifact on both platforms. Their completed results
are retained with the publication record.

## Retained evidence

Evidence is retained under `/private/tmp/acc-058-combined-release` and copied into
the local Git-excluded media archive at `.media/repository/releases/0.5.8/combined/`.
The native capture and failing mutation logs retain their original bytes.

| File | SHA-256 |
|---|---|
| `upgrade.json` | `95e8c408265e79d4a0ab8237d8774e0d7953533414d11bcb291a3e5038e450e5` |
| `published-code-comparison.json` | `2ed796ef34e55deae7fc674cce7450ee26c6e56051259bf4872b66eacfd3d23e` |
| `managed-and-startup.log` | `866f7ebfebef321951d037e51c8b37c6b28a383d2a7b22edb6d1e8e4bbdffeeb` |
| `verify.log` | `92a71887d83a6f9986c64ac8ab94afaf436bb1f11b3fae924c59454cc6d1f5ed` |
| `peer-native-runtime-identity.json` | `b3b96d85fc3088bbdeaa0ea7d3b5aabe388d4ce7b6340bd44a6a380e96f00bb2` |
| `native-capture/evidence.json` | `d42606f226b66ee1223ddfdfa9d62178887f11a5d577f723c6839a388a0da0ef` |

The earlier README-only candidate from `7fb2a3e` was never published. Its archive
and evidence remain in the local history, clearly marked as superseded. Only the
combined clean-source archive identified above is eligible for publication.
