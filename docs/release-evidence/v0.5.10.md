# ACC 0.5.10 candidate evidence

Captured on 2026-09-20 with Node 26.5.1 and npm 11.17.0 on darwin-arm64.

| Item | Value |
|---|---|
| Clean source commit | `b577f5790b9655be0055be14655c8c7203ab560d` |
| Artifact | `agents-can-communicate-0.5.10.tgz` |
| Size / files | 402,274 bytes / 281 files |
| SHA-256 | `56fc37184ab120a801dd84a0f30b2f467ae55feda445559251c1747ba47e6044` |
| SHA-1 | `7858e053f7b3ea33878b0a1afc6eeed79a995c54` |
| npm integrity | `sha512-9ozcNPfujHlv9ZVWz2Cs2D91rwNWDAXCwm9TMNRiMuammS883go0+PhMK1ptR8Qc7ePf6Fh5sHkgQ8z1C7hLBA==` |

The final candidate source contains PR #172, merged at
`53fb1b2b1573d8efd60e6e63de876cc76688f9bf`, plus the version metadata for this
release, plus the Channel test synchronization correction described below. The
root, fourteen workspaces, Gemini extension and lockfile move together. The tree
was clean immediately before packing. The following evidence commit changes only
this file and CHANGELOG, which npm excludes. Verification used the saved archive
by filename.

## Installed artifact

The package verifier passed against the exact archive: 281 entries with none
forbidden, five certification manifests with the exact evidence allowlist, packed
Markdown links resolving inside the tarball, the root and all fourteen bundled
workspaces at 0.5.10, a clean installation exposing the three ACC executables,
doctor reporting five adapters, and operations in a workspace without Git that
wrote no runtime state into the project. Client install/uninstall restored topology,
modes, links and bytes; repeated uninstall was a no-op. Syntax validation covered
555 tracked modules.

All 28 managed-update checks passed against installed archives, with no skips:
enrollment and activation, persistent MCP ownership across an update, integrity
rejection, refresh recovery, and stable launchers after the initial package is gone.

## Published-version upgrade preflight

The public-registry 0.5.9 archive had SHA-256
`bce564ea69700f1f26ef418a51c78929db24e08c23d132b5c0a2e728c2a38ee1`, matching its
release record. It was installed into private home, data and project directories.
Client shims reported Claude Code 2.1.278 and Codex 0.153.4; no vendor service ran.

The fixture recorded actionable delivery choices for both adapters, installed the
exact 0.5.10 candidate over the old copy, and ran `acc update`. The active runtime
became 0.5.10 with phase `ready` and no pending generation. Both saved choices and
their `explicit-option` provenance survived, and both install records moved to
0.5.10. Each versioned plugin cache held exactly 0.5.9 and 0.5.10.

All 281 source, archive and installed files matched byte for byte. Compared with
published 0.5.9, 243 files are identical, 16 differ only by their manifest version,
20 have content changes, and two modules are added; none are removed.

## Regression evidence and limits

The release includes the launch-room, compaction-owner and CLI fixes verified in
[saved room evidence](unreleased-saved-room-reference.md),
[workspace continuity evidence](unreleased-workspace-continuity.md), and
[owner continuity evidence](unreleased-session-continuity.md). Those records retain
their original development source/archive provenance and mutation results.

The new cwd and compaction scenarios use installed-hook fixtures and certify no
new live-delivery capability. The earlier real Claude Code startup capture retains
its original source and archive identity. This upgrade preflight uses client shims;
unavailable vendor services retain the visible inbox fallback.

Legacy conversations without a saved launch binding establish one on their next
startup or user-turn hook. Their original cwd cannot be reconstructed. The release
does not renew historical delivery leases or migrate messages between participants.

## Final gates and retained evidence

Merged-main CI passed Linux tests and package checks on both platforms, but macOS
exposed an existing race in the disposable Channel capture test: stdout reached
the parent before the child appended `notification_accepted`. The test now waits
for Channel shutdown before inspecting the completed observation log. All twelve
Channel tests passed. In an isolated copy, a forced delay reproduced the original
failure; shutdown synchronization passed with that delay, and removing the actual
observation still failed. No production or packed file changed. Repacking from
the final source produced the identical archive digest; exact-package verification
and the published-version upgrade preflight passed again.

The full suite runs after candidate B through the unchanged pre-push hook. GitHub
checks cover Node 24 on Linux and macOS and package installation on both platforms.
Their completed results are retained with the publication record.

The exact archive, pack manifest, package verification, upgrade script/result,
and published-file comparison are retained in the Git-excluded local archive at
`.media/repository/releases/0.5.10/`.
