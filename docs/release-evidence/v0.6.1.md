# ACC 0.6.1 candidate evidence

Captured on 2026-09-22 with Node 26.5.1 and npm 11.17.0 on darwin-arm64.

| Item | Value |
|---|---|
| Clean source commit | `913af545c95976737bcd6d0a4fcd433400524ec1` |
| Artifact | `agents-can-communicate-0.6.1.tgz` |
| Size / files | 445,146 bytes / 306 files |
| SHA-256 | `b6364e3d28ca50f6aa7af77b49919297018917641eed42e97694d7acd12ad6a5` |
| SHA-1 | `7a7f60299d824f6414b48008f3cef432a11fcdd6` |
| npm integrity | `sha512-JUKeZz9Ee+8UI3uorgo3pudpt0iZst2jg8WrIERlNj8QBm16rV62n0jL4YQLDjzNs7ePEWO/0jZdwRtDkSamig==` |

The candidate source is PR #183, merged at `5cd0d1d9157dedb6f705ba2447fd0c0ca53a53d9`, plus
the version metadata for this release. The root, fifteen workspaces, the Gemini extension,
the Antigravity plugin manifest and the lockfile move together. The tree was clean
immediately before packing. The following evidence commit changes only this file and
CHANGELOG, which npm excludes. Verification used the saved archive by filename.

## Installed artifact

The package verifier passed against the exact archive: 306 entries with none forbidden,
six certification manifests with the exact evidence allowlist, packed Markdown links
resolving inside the tarball, the root and all fifteen bundled workspaces at 0.6.1, a clean
installation exposing the three ACC executables, doctor reporting six adapters, and
operations in a workspace without Git that wrote no runtime state into the project. Client
install/uninstall restored topology, modes, links and bytes; repeated uninstall was a no-op.
Syntax validation covered 589 tracked modules.

All 28 managed-update checks passed against installed archives, with no skips.

## Published-version upgrade preflight

The public-registry 0.6.0 archive had SHA-256
`fed73768b83df33a7d5b779358a4907bb3ce880709f4faa5dda97d8c458b6c94`, matching its
release record. It was installed into private home, data and project directories.
Client shims reported Claude Code 2.1.278 and Codex 0.153.4; no vendor service ran.

The fixture recorded actionable delivery choices for both adapters, installed the exact
0.6.1 candidate over the old copy, and ran `acc update`. The active runtime became 0.6.1
with phase `ready` and no pending generation. Both saved choices and their
`explicit-option` provenance survived, and both install records moved to 0.6.1. Each
versioned plugin cache held exactly 0.6.0 and 0.6.1.

All 306 source, archive and installed files matched byte for byte. Compared with
published 0.6.0, 285 files are identical, 18 differ only by their manifest version, and
three have content changes - `core/src/sessions.mjs`,
`storage-filesystem/src/retention.mjs` and `storage-filesystem/src/store.mjs`; none are
added or removed.

## Regression evidence and limits

The release carries the fix verified in
[store lookup evidence](unreleased-store-lookup-performance.md): on a copy of a long-lived
workspace, `acc status` fell from 4.6-6.6 s to 0.47-0.70 s and Antigravity hooks from
5-8.6 s to 0.8-1.5 s. That record retains its original development source/archive
provenance.

Retention markers already on disk stay, by design, and now cost one directory listing per
lookup. Full-path validation of every state read remains the deliberate cost in a large
workspace. No new client capability is certified.

## Final gates and retained evidence

The full suite runs after candidate B through the unchanged pre-push hook. GitHub checks
cover Node 24 on Linux and macOS and package installation on both platforms.

The exact archive, pack manifest, package verification, managed-update log, upgrade
script/result, and published-file comparison are retained in the Git-excluded local archive
at `.media/repository/releases/0.6.1/`.
