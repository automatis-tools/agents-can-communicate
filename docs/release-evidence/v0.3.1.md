# ACC v0.3.1 release evidence

This release carries no new capability capture, and says so plainly. What it changes is what
a person reads and what a peer name resolves to: the one consent question asked at install,
the way `--to` accepts the name of a client, and the documentation that had been describing
three different products at once. The transport that 0.3.0 certified is byte-identical here,
so this document records what was verified now and what is carried forward unretested.

## Candidate identity

| Fact | Value |
|---|---|
| Candidate commit | `df7d246fe86a360ab4386a7409977b54e75c83dd` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `256236` |
| Entries | `196` |
| sha256 | `2de2aa9f55053baac078d181423984ee2e91660b7945ad8f8bbba7b2ddcbbb0a` |
| Bundled workspaces | `14`, all version `0.3.1` |
| Node | `26.5.1`; the package requires `>=24` |
| npm | `11.17.0` |
| Platform | macOS `26.6.2`, darwin `25.6.0`, arm64 |

```bash
npm pack --pack-destination "$candidate_dir"
node scripts/verify-package.mjs "$candidate_dir/agents-can-communicate-0.3.1.tgz"
```

Result: PASS, exit `0` — 196 non-forbidden entries, five certification manifests with
exactly their referenced fixtures, every packed Markdown link resolving inside the tarball,
fourteen bundled `0.3.1` workspaces, linked `acc`, `acc-hook` and `acc-mcp`, five adapters
reported by doctor, a coordination cycle in a workspace with no Git, no project state left
behind, and a client home restored to its exact prior topology, bytes and modes with a
second uninstall as a no-op.

The tree was clean when this was packed, so the verifier printed no dirty-tree warning. It
was handed the saved tarball rather than the working tree and therefore reported
`revision unknown`; the candidate commit above was captured from a clean tree immediately
before packing, which is what ties those bytes to that commit.

## What actually changed in shipped code

Two source files, and nothing else under `packages/*/src/` or `bin/`:

- `packages/cli/src/install-command.mjs` — the native-delivery consent question. It had
  opened with internal artefact names and never said what the reader lives with, which is
  that the client will ask to allow development channels at every start.
- `packages/core/src/conversations.mjs` — a `--to` name matching no participant is now tried
  as a client name. One live session of that client resolves; two refuse and name them; an
  exact participant id is never overruled.

Alongside them, the five `SKILL.md` files each name a peer client rather than their own, and
the entry documentation was rewritten. Skills and documentation are packed, so they belong
to this artifact even though they run no code.

## Carried forward from 0.3.0, unretested here

No file under `packages/adapter-claude-code/src/`, `packages/delivery-router/src/`,
`packages/hook-runner/src/` or `bin/` differs from `v0.3.0`. The native transport, the
durable queue and the hook entry points are therefore the same bytes that 0.3.0 certified,
and this release makes no new claim about them:

- Claude Code native live delivery stays anchored at its captured `darwin-arm64` minimum,
  with the per-session handshake and the vendor development-channel warning unchanged.
- Codex live delivery remains withdrawn; its queue capture stands, and the mode that would
  reach it still hides the session's workspace identity.
- Gemini CLI's hook certification at `0.57.0` and Kimi Code's at `0.36.1` are carried
  forward untouched.

Carried forward is not re-verified. A reader who needs a fresh capture should take one; the
procedure is in [Releasing](../RELEASING.md).

## Not covered

- No real-client session was driven for this release. The consent question's new wording was
  exercised by the suite, not by a person installing into a live client.
- `darwin-x64`, Linux and Windows still carry no native capture. Windows remains an explicit
  unsupported-platform skip.
- Client-name addressing was verified by unit and acceptance tests, including the gate that
  checks every `--to` in the documentation and the skills. It was not exercised between two
  live vendor clients as part of this release.


## Unreleased MCP compatibility candidate — 2026-09-06

This is a later local development candidate, not a replacement for the published record
above. No tag, publication, push or merge was performed.

| Fact | Value |
|---|---|
| Candidate commit | `44ff112ac9c08b95684fa9eaabddf893c7d19483` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `257639` |
| Entries | `197` |
| sha256 | `cac6738e3c3a0481319d8b3333c3dab14b6579ebc5f5d91d27eceedce2915391` |

The tree was clean immediately before packing this saved tarball. The verifier was passed
that exact file and exited 0: package contents and links, all 14 bundled workspaces,
installation, doctor, a non-Git coordination cycle, and reversible client install/uninstall
passed. Node 26.5.1 on macOS arm64. The package still requires Node >=24.

Manual MCP was driven through real Codex CLI 0.153.4 (protocol 2025-06-18) and Claude Code
2.1.263 (2025-11-25), with invocation-local tool permissions and isolated workspace/data
roots. Both executed status and inbox successfully. A subsequent live pair completed a
review, clarification, code correction and independent 7-test run; both exited 0. Replacing
the example's corrected implementation with its original defect caused 3 of those 7 tests
to fail. A fresh Claude session recovered the handoff without prior conversation or files.
These live runs used the same MCP logic before the final documentation and comment edits;
the exact saved final tarball separately passed installed-artifact verification.

Three protocol mutations were detected: removing initialized support, restoring array
structuredContent for 2025 clients, and admitting malformed client capabilities. The first
full suite had only the expected stale-candidate-record failure before this evidence update.
The final full-suite gate runs after this evidence commit, as required by the release
procedure; this record does not pre-claim its result.

Observed limits: explicit task prompts and polling; no native push, guard or lifecycle
recapture. A future participant that has never joined cannot be directly addressed; the
author recovered by recording a workspace handoff, later found with a full sync. Final
post-work messages remained queued when their recipients had already stopped. The 2026
interface has automated protocol coverage, not a captured real 2026 client in this run.

## Unreleased malformed-request candidate — 2026-09-06

This later local candidate includes the MCP compatibility fix above and rejects malformed
input without terminating stdio. Earlier artifact records remain historical evidence.

| Fact | Value |
|---|---|
| Candidate commit | `d919220b36681c07097b9340e8d44d45867e66e8` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `257818` |
| Entries | `197` |
| sha256 | `81ffc440f01cd4e517df70bcff1bcfa18e9d97c3153a829220b16e5b3c1943b8` |

The tree was clean when the saved tarball was packed. Passing that exact tarball to
`scripts/verify-package.mjs` returned exit 0: package contents and links, all 14 bundled
workspaces, installation, doctor, a non-Git coordination cycle, and reversible client
install/uninstall passed. Platform: Node 26.5.1, npm 11.17.0, macOS arm64.

A separate clean consumer installed this same tarball offline. Its `acc-mcp` process
received `null`, malformed JSON, an invalid method with a usable ID, invalid parameters,
and a valid ping. It returned the four expected errors with the appropriate IDs, answered
the ping, and exited 0. Before the fix, the first line terminated the installed server.

Before packing, all 68 focused MCP and installed-client tests passed. Four exact mutations
were rejected by the new raw-stream and packed regressions: removing envelope validation,
omitting the parse-error ID, removing parameter validation, and discarding usable request
IDs. Restoring the fix returned all 20 checks in those two files to passing. Syntax checking
covered all 321 tracked modules. The final full suite runs after this evidence commit;
its result is not pre-claimed here.

No adapter capability or certification changed. This is a local candidate, with no push,
merge, tag or publication.

## Unreleased CLI ownership candidate — 2026-09-06

This local candidate withdraws unsafe implicit CLI ownership. Published records and earlier
candidate captures above remain historical evidence. No push, merge, tag, or publication.

| Fact | Value |
|---|---|
| Candidate commit | `13c8017d0a3bdc5c670734f22b3b65ec36e5f533` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `257748` |
| Entries | `197` |
| sha256 | `a4072ec147917dd5333b19e2b70f1477e004fc8e79f80f2543b771c3903cc410` |

The tree was clean before packing. The exact saved tarball passed
`node scripts/verify-package.mjs`: 197 allowed entries, 14 bundled workspaces, Markdown
links, executable installation, doctor, non-Git coordination with no project runtime state,
and exact client-home restoration with idempotent repeated uninstall. Node 26.5.1 / npm
11.17.0 on macOS arm64. The required runtime remains Node >=24.

A real Claude Code 2.1.263 session loaded the candidate plugin, then launched Codex CLI
0.153.4 as a nested client. Codex naturally inherited Claude's native session ID while
exporting its own. Each client attempted work, work with a peer's public session selector,
inbox, and message without ACC owner credentials: all returned exit 2 /
`caller_identity_unresolved`, with identical durable snapshots before and after the rejected
calls. Each then used the pair returned by its own manual attach. Intent and claims named
the correct owner; a Codex request received a Claude reply and an acknowledged receipt.
Both manual sessions finished. These were scripted calls from real client shells, not a
claim that ordinary tasks coordinate automatically. The live-test tarball and this clean
candidate have the same SHA-256.

Codex was intentionally launched with `--ignore-user-config` to exercise the missing-hook
caller, retaining normal authentication and the workspace-write sandbox. No hook-trust
bypass or persistent client-config changes were used. Claude's plugin was invocation-local;
all project and runtime data were disposable. Capture retained event metadata, a fixed
native-ID allowlist, explicit diagnostic messages, and final diagnostic results, not raw
transcripts or prompts. No native delivery capability was promoted or re-certified.

Twenty-nine focused assertions/tests passed across owner-unit and installed-artifact
regressions. Restoring implicit peer selection, allowing selector redirection, replacing a
supplied generation, or exempting inbox from ownership each made the regressions fail.
An inherited test-runner ACC pair is explicitly cleared by the packed fixture; its isolation
regression passed with those host variables deliberately set.

The remaining product limitation is explicit: hooks do not provide CLI owner credentials.
A generic MCP connection or new manual attachment may represent a different participant
from hook-managed presence and cannot inherit that participant's pending inbox. Existing
positive process fixtures now supply their own credentials; they do not certify that a
native client supplies them. Safe automatic handoff of per-session identity remains future
work. Process IDs and inherited environment values are not substitutes for that contract.

The final full-suite gate runs after this evidence-only commit, as required by the release
procedure; this record does not pre-claim its result.


## Hook-provided CLI owner candidate, 2026-09-06

| Fact | Value |
|---|---|
| Candidate commit | `ac5d7afe09a584467f9417d2badd203e6fcd4e26` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `258635` |
| Entries | `197` |
| sha256 | `af66590f496d8d9d591212b194b61be0e8eb1afd27d0c7485e60beff1b006d43` |

Packed from a clean worktree. The exact saved tarball passed `verify-package.mjs`:
all 14 bundled workspaces, package contents and documentation links, installation,
doctor, non-Git coordination, and reversible client install/uninstall. Node 26.5.1,
npm 11.17.0, macOS arm64; the supported minimum remains Node 24.

The hook emits its own session/generation pair outside untrusted peer bodies. The
installed skill appends it to CLI calls, including the now-accepted explicit flags on
status and sync. CLI inference from peer bindings, native environment IDs, checkout or
PID remains disabled. A queued-body recovery command retains its budget; a complete pair
that cannot coexist with it is omitted with an explicit diagnostic. Quiet solo turns
remain silent even when the session owns a claim. Optional overflow counts are discarded
before the only usable inbox command.

Real Claude Code 2.1.263 and Codex CLI 0.153.4 loaded the installed skill and used their
hook-supplied pair for intent, claims, inbox/reply, and finish in a disposable workspace.
Claude left a question and the later Codex session answered it; durable author session IDs
matched the native hook sessions, the question receipt became acknowledged, and claims
were released. The diagnostic supplied no owner credentials in either task prompt and
neither model attached another session. Codex hooks were reviewed through the native UI;
plugin enablement by itself was insufficient. Both real clients were rerun against the
exact final candidate above, installed in the same disposable consumer. Event actors were
checked for intent, claim acquisition/release, message recording and acknowledgement in
both native sessions; final status reported zero live sessions and zero claims. All five
original Codex hook-trust records were restored, and the temporary candidate cache and
disposable project-trust entry were removed after byte comparison.

Codex also attempted an additional exact inbox read after its successful reply; that call
returned exit 5. It then completed status and finish successfully. Bounded metadata did not
retain that call's selector or error body, so this capture does not establish its exact
rejection reason or prove every read interaction succeeds. The core deliberately refuses
exact recovery of an already-acknowledged message; making resolved-message inspection
clearer remains a separate usability concern.

This was sequential durable communication, not idle wake or live injection. Capability
flags and certification versions were not promoted. Client execution permission and active,
trusted hooks are required; standalone CLI calls still need an explicit owner pair.

Seven packed regression cases cover emitted-owner calls, status/sync attention, 84/120/150/
200-byte budgets, same-client ownership, restarted credentials, and own-claim solo silence.
The projector separately exercises exact recovery when an optional count cannot fit.
Before fixes, the corresponding cases failed. Five final mutations also failed: removing
the owner header, removing its byte reserve, restoring old overflow projection, removing
status flags, and removing sync's generation flag. Every mutation was restored before
packing. The whole-suite gate is recorded only after the evidence commit.

## Reply receipt inspection candidate, 2026-09-06

| Fact | Value |
|---|---|
| Candidate commit | `15bc261550c584798691536f1ae55301c8c2b88c` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `259401` |
| Entries | `197` |
| sha256 | `585035ad54e3b208ff52e8558bf2171f148389036325229f3269c56326c33e5d` |

Packed from a clean committed worktree. The saved tarball passed `verify-package.mjs`:
197 permitted entries and links, 14 bundled workspaces, executable installation, doctor,
coordination without Git or project runtime writes, and reversible client install/uninstall.
The final tarball is byte-identical to the installed candidate used by both real clients
below. Platform: Node 26.5.1, npm 11.17.0, macOS arm64. Minimum runtime remains Node 24.

The earlier Codex exit-5 observation was reproduced against the preceding candidate with
bounded selector and error metadata. Codex successfully replied, then selected the original
request ID, and received `that message is not recoverable by this participant`. Core inbox
selection excluded acknowledged receipts; CLI and MCP also discarded the original receipt
already returned by core reply. This capture establishes the rejected selector, rather
than inferring it from a successful outgoing reply.

Exact acknowledged inspection now returns the participant's original message and receipt
without advancing state, changing timestamps, or emitting an event. Ordinary inbox still
returns unresolved mail only. Participant ownership and an open current generation remain
required. CLI/MCP reply results expose the original receipt separately from the outgoing
message and delivery; the human CLI response identifies both messages. All five installed
skills and the protocol, CLI and MCP references describe these semantics.

Real Codex CLI 0.153.4 ran with `--ignore-user-config` and created its own manual diagnostic
attachment. Its initial attempt guessed unsupported attach options and did not reach the
target flow. A subsequent prompt supplied the documented attach syntax, with no credentials.
Codex read the original request, replied with an acknowledged original receipt, inspected
that original successfully, and observed an empty ordinary inbox. It corrected an invalid
finish status and then finished. This proves the owned CLI flow, not native Codex hooks or
error-free discovery of the CLI. The baseline also rejected an invalid work mode.

Real Claude Code 2.1.263 loaded the temporary installed plugin and skill. Its native hook
supplied the owner pair; neither the prompt nor a manual attach supplied credentials.
All observed Claude tool/CLI commands succeeded: inbox, reply, exact original inspection,
empty ordinary inbox, and finish. The original receipt remained acknowledged with the same
timestamp; the outgoing reply had its own message ID and queued delivery to the scripted
peer. No unread reply was described as retrieved or acknowledged by its recipient.

Both scenarios used disposable non-Git projects, isolated runtime homes, and scripted peers.
Their sessions and claims were closed afterward. No persistent client configuration was
changed. Only command categories, bounded diagnostics, selector classifications, receipt
states and final diagnostic summaries were retained; no raw client transcripts were saved.
This run does not certify idle wake, injection, guard behavior, or any new adapter version.

Before the fix, the new core and installed CLI/MCP regressions failed. All 81 final focused
tests passed. Seven restored mutations were caught: hiding acknowledged messages, attempting
to regress them to retrieved, refreshing the inspection timestamp, removing the participant
filter, dropping the CLI receipt, dropping the human acknowledgement text, and dropping the
MCP receipt. An independent code review found no actionable defect. Syntax checks covered
326 tracked modules. The final full suite runs after this evidence-only commit; this record
does not pre-claim its outcome. No push, merge, tag or publication.

## Reply receipt final gate correction, 2026-09-06

| Fact | Value |
|---|---|
| Candidate commit | `153943af838b5e3be702a3dd2a6e971cb8c71bca` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `259401` |
| Entries | `197` |
| sha256 | `585035ad54e3b208ff52e8558bf2171f148389036325229f3269c56326c33e5d` |

The preceding candidate's full suite ran 1467 tests: 1464 passed, one failed, and two
cases were skipped because Gemini was installed and this platform had a passing capture.
The failure was an older CLI integration assertion
requiring exactly `delivery` and `message`, rejecting the newly returned original receipt.
The assertion now checks the original message ID, the expected recipient, and acknowledged
state. Both removal of the receipt and substitution of the recipient caused that corrected
test to fail. Both mutations were restored, all 12 CLI integration tests passed, and syntax
checking again passed for 326 tracked modules. No production or packed file changed.

The clean committed tree was repacked under this source revision. Its tarball is
byte-identical to the previous candidate and to the artifact exercised by real Codex and
Claude above. `verify-package.mjs` passed again on this exact saved tarball, including clean
install, non-Git coordination and reversible client installation. The platform and observed
client limits above still apply. The full suite is rerun after this evidence-only commit;
its result is not pre-claimed here. No push, merge, tag or publication.

## Command help candidate, 2026-09-06

| Fact | Value |
|---|---|
| Candidate commit | `1096e3243567de8ea8f396d06a93519a0a908c23` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `261461` |
| Entries | `197` |
| sha256 | `f5b342e3ec4f989aeba5ae074e064092f4daba4a8a85df12fab66015e019c650` |

Packed from a clean committed worktree. The exact archive passed `verify-package.mjs`:
permitted contents and links, 14 bundled workspaces, clean executable installation, doctor,
non-Git coordination without project runtime writes, and exact client-home restoration with
idempotent repeated uninstall. Node 26.5.1 / npm 11.17.0 / macOS arm64; minimum Node 24.
Its bytes match the final installed candidate exercised by the real client below.

Per-command help now reports required, optional, repeated and flag options, accepted values
and usage notes in text or JSON. Both command-local `--help`/`-h` and `help <command>` work,
including config subcommands. Help is recognized only at option boundaries, leaving literal
help-looking values intact. It routes around workspace, ownership and execution even when
the store cannot open. Command and alias lookup require own properties, so inherited names
such as `constructor` and `toString` cannot become commands or corrupt their diagnostics.
Intent, claim and handoff choices share the durable schema's constants; accepted record
values and schema version did not change.

Real Codex CLI 0.153.4 received the same bounded diagnostic on the preceding reply-receipt
artifact and on the new candidate: discover syntax through built-in help, create its own
manual participant, publish reviewing intent, read/reply, and finish with a complete handoff.
The prompt supplied the installed executable and participant name, but no attach/work/reply/
finish argument syntax or owner credentials. On the old artifact, general help succeeded,
but five command-local help requests and `help attach --json` failed with exit 2. The client
reported insufficient help, did not attach, and left the request queued.

On the final candidate, all 11 CLI calls exited 0. Durable records confirmed the model's
own participant published `review` intent, acknowledged the original by replying, recorded
a `complete` handoff and closed its session. The scripted peer then detached; final status
had zero live sessions and zero claims. An earlier candidate also completed this flow; the
final client rerun includes both inherited-name corrections. Queued outgoing messages were
not presented as proof that their recipient had read them.

The client used `--ignore-user-config` and a workspace-write sandbox in a disposable non-Git
project. This is manual CLI discovery, not a native hook, wake, injection or guard capture.
Persistent client configuration and authentication were not modified. The diagnostic kept
bounded command categories, exit/error metadata, derived coordination facts and a final
diagnostic summary, not raw client transcripts. No adapter certification was promoted.

The new installed tests first failed on missing command help; inherited-command and alias
diagnostic cases separately failed before their fixes. Ten deliberate mutations were caught:
losing either help spelling, opening the workspace for help, intercepting a message body,
omitting mode choices or optional flags, hiding human-readable values, dropping required-body
validation, accepting inherited command names, and resolving inherited aliases. Every
mutation was restored. Independent review findings about config --force wording, required-body
test specificity and inherited alias diagnostics were resolved and rereviewed.

CLI/protocol/docs checks passed 298 of 299 cases; the missing-Gemini uninstall case was
skipped because Gemini is installed. After the last alias correction, all 28 parser/help/
installed regressions passed. Syntax checking covered 327 tracked modules, and npm ci
installed all 14 workspaces. The full suite runs after this evidence-only commit; its result
is not pre-claimed here. No push, merge, tag or publication.

## Command-help review and solo-owner candidate, 2026-09-06

| Fact | Value |
|---|---|
| Candidate commit | `9d3ae91315f4e2836e72dc206500e0d5529171ce` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `261952` |
| Entries | `197` |
| sha256 | `5c0a75056946cd01730668b58e92895748153c2e7cfc4d32e7abf32c7c1ea3fe` |

Packed from a clean committed worktree on Node 26.5.1 / npm 11.17.0 / macOS arm64.
The minimum runtime remains Node 24. The exact saved archive passed verify-package:
197 permitted entries, all packed Markdown links, five certification manifests, 14 bundled
workspaces, clean executable installation, doctor, non-Git coordination without repository
runtime writes, and exact client-home restoration including a no-op repeated uninstall.

Commit ffec3d754825eff641a493f5a650a15b8d915240 corrects command-specific help after a
real Claude review: config subcommand detail, rejection of init-only validate flags, adapter
choices from the installer registry, only effective workspace globals in help, and reachable
self-help. Ignored global selectors remain accepted for wrapper compatibility. All 37 focused
parser/help/packed tests passed. Nine mutations each failed their intended regression, with
37 tests passing before and after restoration. Adjacent CLI checks had 25 passes and one
explicit missing-Gemini skip because Gemini was installed.

That earlier ordinary review used stock Claude/Codex plugins on the preceding command-help
artifact. Claude returned findings; Codex retrieved them and fixed the code. Claude had
already exited, so a fresh real Claude continuation reviewed the final changes and returned
APPROVED with no blocking findings. The author's original process reached the harness's
15-minute deadline while waiting. The final approval was recorded, but queued to that now
inactive author. This was an operator continuation, not unattended completion by the two
original processes. It is distinct from the successful delayed-start run below.

Commit 9d3ae91315f4e2836e72dc206500e0d5529171ce fixes a startup-order defect exposed by that review:
the first session's solo turn suppressed the owner header, so an inbox call after a peer
joined failed with caller_identity_unresolved. An installed reproduction confirmed that a
later user prompt supplied the missing pair while PreToolUse did not. Turn hooks now supply
only their own complete pair when no coordination body exists, including the own-claim path.
They warn and emit empty context if that pair cannot fit. Pending-message recovery still
has priority when the budget cannot fit both it and the owner line; receipt commits are
unchanged. Codex and Kimi no longer add a newline to raw injected context. Review found
both byte-boundary bugs; each new packed test first failed with 106 bytes against a 105-byte
ceiling. Claude's envelope already preserves the content byte budget.

All 105 final focused hook/adapter/packed tests passed, with no skips. Eight exact restored
mutations were caught: dropping solo identity, dropping own-claim identity, rejecting the
exact budget, allowing overflow, substituting session ID for generation, hiding the budget
warning, and restoring Codex's or Kimi's extra newline. Wrong generation fails through actual
inbox/reply behavior. Independent review reported no remaining code blocker. Syntax checking
passed for 328 tracked modules and npm ci installed all 14 workspaces.

Real-client observation used a separately packed development artifact with SHA-256
f6d183f13ef0e8cac15060e256335597ea602a3b97559ee54f7ba7fde42466a4. Its runner, Codex hook
and both native clients' skill sources were compared byte-for-byte with this correction.
The final archive above additionally contains Kimi's newline fix and the compatibility
record; it is not falsely presented as the exact archive used by that native run.

With a fresh non-Git project and empty runtime, Claude Code 2.1.263 began a small review
alone. The harness launched Codex CLI 0.153.4 after Claude's first tool result. Codex
implemented the fixture, ran seven tests and sent a review request. Claude retrieved it,
independently reviewed the code and sent APPROVED. The original request became acknowledged;
Codex retrieved the answer, recorded a complete handoff and exited successfully. Claude
observed the handoff, cleared its intent and exited successfully. There were exactly two
native participants, no extra manual attachment, no scripted peers, no supplied owner
credentials, and no operator messages or continuation prompts. The clients selected inbox
polling themselves; delivery was off. This is an observed in-turn CLI interaction, not an
idle-wake or live-push capability capture.

The first fixture launch omitted Codex exec's normal non-Git option and stopped before
Codex created a session. The successful retry used --skip-git-repo-check and a new empty
runtime. Codex's five plugin hooks were reviewed in its native trust UI; after the completed
run, temporary cache removal and restoration of the original configuration were verified
by byte-level SHA equality. No auth files were copied. Only bounded tool metadata, final
diagnostics and durable ACC facts were retained, not raw transcripts.

Two additional real solo Claude runs used the same package-name/version request. The old
broad skill trigger loaded ACC before reading the file; the narrowed trigger only read it.
Both returned the correct answer and issued no coordination command. This is one observed
before/after sample. Adapter capability flags and exact-version certification remain unchanged.

The full suite runs after this evidence-only commit; this record does not pre-claim its
outcome. No push, merge, tag, publication or client usage-limit reset was performed.

## Session generation and restart candidate, 2026-09-07

| Fact | Value |
|---|---|
| Candidate commit | `9a7287fe24cb65423dede98b27af807bd5897459` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `262315` |
| Entries | `197` |
| sha256 | `84209b478b02e4d7a8943f939a958581b8b8791e3ff60d81cabdca101a690c68` |

Packed from a clean committed worktree on Node 26.5.1 / npm 11.17.0 / macOS arm64.
The minimum runtime remains Node 24. The exact saved archive passed verify-package:
197 permitted entries, all packed Markdown links, five certification manifests, 14 bundled
workspaces, executable installation, doctor, non-Git coordination without repository
runtime writes, and exact client-home restoration with a no-op repeated uninstall.
The preceding candidate's full suite completed after faaf8f4: 1483 tests, 1481 passed,
zero failed and two explicit skips. That count does not describe this candidate.

An installed probe against the preceding archive found that a sequential native-hook restart
already recovered a pending request and rejected the old pair. Scheduling close/replacement
between owner precheck and commit exposed different failures: inbox and acknowledgement
accepted the old operation and changed receipts; heartbeat and close overwrote the successor's
generation with an old record. Reply already rejected that race. The scheduling wrapper only
controls when real lifecycle calls run; storage and transactions are the installed filesystem
implementation, not mocked records. The corrected archive rejects all five old operations
with exit 5, preserves successor state and events, and retains sequential recovery.

Commit 1f12223 revalidates inbox and acknowledgement ownership under the transaction lock.
The durable session takes precedence, with an ephemeral fallback for valid empty solo inboxes.
An independent review caught that fallback missing from the first implementation; the packed
CLI test reproduced exit 5 before the correction. All 23 focused checks passed. Five exact
mutations were caught: removing either commit-time check, accepting another generation,
accepting a closed owner, and refusing a valid solo owner. Restored checks passed.

Commit 9a7287fe24cb65423dede98b27af807bd5897459 validates heartbeat and close against the
record read under the writer lock. These operations preserve current metadata and reject
workspace mismatch; closed sessions reject heartbeats. Conditional ephemeral deletion evaluates
its guard under the same mutex, including delayed intent cleanup that must preserve a successor.
Memory storage supports the same guard contract. The original 11 packed lifecycle scenarios
failed before the fix. Review caught an explicit workspace-scope regression; four additional
packed cases failed before restoring that check. The final 42 focused checks and 253 adjacent
core/storage/process checks passed without skips. Nine exact restored mutations were caught:
stale generation, closed heartbeat, stale heartbeat metadata, stale close metadata, successor
intent deletion, ignored filesystem guard, ignored memory guard, guard before lock, and missing
workspace validation. Independent review found no remaining blocker. Syntax passed for all
331 tracked modules, and npm ci installed all 14 workspaces.

The real-client run used the archive with the same SHA-256 as the final candidate above.
Additionally, 61 shipped modules across core, storage-filesystem, hook-runner, CLI and both
native adapters were compared byte-for-byte. The compatibility observations are repository-only
files and do not alter npm bytes.

Real Claude Code 2.1.263 and Codex CLI 0.153.4 used stock plugins with delivery off in a
non-Git fixture. The author requested review. Immediately after the reviewer's inbox returned,
the controller paused that exact Claude process and confirmed the request was retrieved with
no reply recorded. It resumed the process and sent SIGINT; the native close hook completed.
The controller launched a fresh Claude conversation at the same configured public participant
address, without the old transcript, message ID or owner credentials. The successor recovered
the request through ACC, independently read the spec and implementation, ran all eight fixture
tests, and replied APPROVE. The request became acknowledged; the original Codex process
retrieved the verdict and recorded a complete handoff. There were two participants, three
closed sessions and no remaining claims. A CLI inbox call with the old pair returned exit 5.

This observes graceful interruption and recovery with a stable participant address. It is
not an abrupt-crash capture. ACC did not launch or restart a client, and no idle-wake,
live-push or certified capability claim changed. The model run proves recovery; deliberately
scheduled installed tests prove the exact commit-time races.

The first harness attempt started the reviewer too early; it ended while still waiting,
before a request existed. The second reached a request but failed because the controller
read a diagnostic JSON file during a non-atomic rewrite. Neither is a passing restart capture.
The third used atomic metadata writes, direct child-PID events and a fresh runtime. Fixture
code from the second attempt was reused; Codex verified it and added failure-path immutability
coverage. Some initial model-selected CLI calls required correction; this is not a claim of
error-free command selection. The bounded diagnostics do not establish every failed call's
cause. Only tool metadata, final diagnostics and durable ACC facts were retained, not raw
transcripts. The five temporary Codex trust records and cache were cleaned up; the original
configuration was restored byte-for-byte. No auth files were copied.

The full suite runs after this evidence-only commit; its outcome is not pre-claimed here.
No push, merge, tag, publication or client usage-limit reset was performed.

## MCP finish retry correction, 2026-09-07

| Fact | Value |
|---|---|
| Candidate commit | `767b2265424c467523f8592ab1be351cbee748b4` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `262425` |
| Entries | `197` |
| sha256 | `7ee404cae17b925ebf4a1c3b7f559266bedce3ec5566c57fccb14b8480f622cf` |

The generation-race candidate's full suite on 303d494 ran 1507 tests: 1504 passed,
one failed and two explicitly skipped. The failure was a real compatibility regression:
MCP resolved every tool call by heartbeating its bound session. Rejecting a closed heartbeat
made an identical finish retry clear that binding and open a new owner, so core correctly
refused the predecessor's clientMessageId. The existing stdio test and a new installed-binary
test both reproduced that failure before correction.

Commit 767b2265424c467523f8592ab1be351cbee748b4 keeps the matching bound generation for
acc_finish and lets core validate it under the transaction lock without a prior heartbeat.
Review found that an initial closed-only shortcut still raced a competing finish after its
lookup. A second installed test scheduled a real finish at that boundary and reproduced
the conflict before the final correction. Other tools retain ordinary heartbeat/reopen
resolution. Neither the core's closed-heartbeat rejection nor generation validation was
relaxed.

All 119 focused MCP, core finish, inbox, lifecycle and storage checks passed with no skips.
The two new packed tests verify sequential and restarted replay, unchanged snapshots and
events, conflicting payload rejection, ordinary new work, cross-generation refusal, a
stale persisted binding, and the concurrent finish boundary. Four exact mutations failed
their intended tests: forcing finish through heartbeat, allowing the shortcut for ordinary
work, lending the current generation to a stale binding, and restoring the closed-only
shortcut. Baseline and restored runs passed. Independent final review found no blocker.

The clean candidate was packed on Node 26.5.1 / npm 11.17.0 / macOS arm64; syntax passed
for 332 tracked modules. Its exact archive passed verify-package: 197 permitted entries,
resolved packed links, five certification manifests, 14 bundled workspaces, executable
installation, doctor, non-Git coordination without project runtime writes, and exact
client-home restoration including repeated uninstall.

A separate clean consumer installed that exact saved archive. Protocol peers using
2025-11-25 and 2025-06-18 completed finish replay before and after a stdio process restart,
with unchanged snapshots/events. A changed payload and a predecessor's key after new work
were rejected. These are real installed MCP exchanges, not live model captures.

Comparing the extracted archives found the same file inventory and only one changed file:
node_modules/@agents-can-communicate/mcp-server/src/server.mjs. The core, CLI, hooks and
adapters used by the real graceful-restart capture above retain identical bytes. That
capture remains evidence for the preceding archive; real models were not rerun for this
MCP correction. No adapter capability flag or certification changed.

The full suite runs after this evidence-only commit; its outcome is not pre-claimed here.
No push, merge, tag, publication or client usage-limit reset was performed.

## Intent, claims and materialisation boundaries, 2026-09-07

| Fact | Value |
|---|---|
| Candidate commit | `c58063a98a67d5f93b407178d6e821eafe2e5abe` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `262877` |
| Entries | `197` |
| sha256 | `81ac8d37b651e7c3982efb9b707060a292a986a6f115873b079cc38c9209be36` |

The preceding candidate's full suite on 67df05e completed with 1509 tests: 1507 passed,
zero failed and two explicit skips. Those results describe the preceding code, not this
candidate. No published record or earlier native-client capture is replaced by this entry.

Installed tests reproduced eight claim races: acquire, renew, release and force release
could commit after the actor closed or its generation was replaced. The initial fix
rechecked the owner inside the session+claim transaction. Independent review then exposed
the first solo claim: materialisation staged ephemeral records before the writer lock,
so it could restore the old owner even before that new check ran. Both close and replacement
cases reproduced this second boundary.

Eight installed intent cases also reproduced stale set/clear writes, across ephemeral and
durable storage. Separate promotion checks showed intent changes lost before promotion
commit or before the pending ephemeral write; heartbeat, resume and close also lost changes
between durable promotion and cleanup. During that interval, replacement was incorrectly
refused because lookup preferred the old ephemeral copy. Three additional cases reproduced
solo intent and first-claim calls accepting a different explicit workspace.

Commit 20a1da5 makes materialisation read current ephemeral records inside the shared writer
transaction. Intent set/clear validate under their ephemeral write lock or session+intent
transaction, switching to the durable path if promotion wins. Clear handles a missing copy,
whose delete guard is not invoked. Session lifecycle writes skip already-promoted copies;
lookup prefers the durable owner and scopes its ephemeral fallback to the selected workspace.
Commit c58063a98a67d5f93b407178d6e821eafe2e5abe guards all claim mutations inside their
session+claim transaction. No storage port API, dependency or adapter capability changed.

All 75 focused checks and 260 adjacent core/MCP/hook/process checks passed without skips.
The three new packed files contain 33 test/subtest results, including positive successor
operations, valid empty solo state, pending-copy cleanup, and unchanged protected claims,
intent and events. Tests use the installed core and filesystem store; scheduling wrappers
only choose when real competing operations run. They do not mock records or transactions.
Independent final review reported no blocker and reverified both first-claim races.

All 21 exact mutations failed their intended installed tests, with baseline and restored
runs passing: three claim commit guards, claim generation and closed-state checks; two
durable intent guards, two ephemeral intent guards, missing-delete revalidation, intent
generation and closed-state checks; three intent promotion/fallback decisions; workspace
scoping, durable lookup precedence, heartbeat/resume/close routing, and staging before lock.
These demonstrate deterministic interleavings, not their frequency in live model sessions.

The candidate was packed from a clean committed worktree on Node 26.5.1 / npm 11.17.0 /
macOS arm64. npm ci installed all 14 workspaces, and syntax passed for 335 modules.
The exact saved archive passed verify-package: 197 permitted entries, resolved packed links,
five certification manifests, 14 bundled workspaces, executable installation, doctor,
non-Git coordination without project runtime writes, and exact client-home restoration
including repeated uninstall.

A separate probe clean-installed both this exact archive and the preceding 7ee404ca archive.
The earlier package accepted stale intent set, intent clear, claim release and first solo
claim, losing successor state or restoring the closed owner. This archive rejected all four
with exit 5 and preserved the intended state. Both packages passed the ordinary installed CLI
attach/work/claim/release/clear/finish cycle, ending with a closed owner, no claims, cleared
intent and no files inside the non-Git project. The first probe draft used an invalid claim
mode; it was corrected before retaining the passing before/after comparison.

Lazy materialisation remains preparation for the first durable object. A rejected first
claim may already have created workspace metadata; the tests establish that it cannot
restore a closed owner or record a stale claim, not full rollback of that preparation.
No native model client was launched for this core correction. Earlier live captures retain
their original artifact provenance, and no new adapter capability or certification is claimed.

The full suite runs after this evidence-only commit; its outcome is not pre-claimed here.
No push, merge, tag or publication was performed.


## Session acquisition and hook reattachment, 2026-09-07

| Fact | Value |
|---|---|
| Candidate commit | `ac8a1540b416372c5f5442bfaa7dd476c7f1e53a` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `263520` |
| Entries | `199` |
| sha256 | `f223c9b55d5fc6ea23e4d268621f54b48de090266769b3d9eb4a5f74dd05d183` |

The preceding archive accepted scheduled same-id opens that overtook a successful owner,
both before and after materialisation. At promotion it could return a generation absent
from the durable state. Six concurrent installed CLI attaches all returned exit 0, while
five of their owner pairs immediately failed heartbeat. Distinct session ids worked.
Opening and resuming also accepted the wrong explicit workspace; ephemeral participant
creation overwrote an existing participant's name and creation time.

Commit 9bd5b2f checks acquisition under the ephemeral writer mutex or durable transaction,
routes delayed opens through promotion, preserves participant metadata and scopes opening.
The extracted opening module stays below 300 lines. Installed filesystem tests replace the
old memory-only promotion test. Commit 6130483 scopes the shared resume predicate before
metadata changes in both stores.

Four independent acc-hook processes with one native id created four live ACC sessions in
the preceding archive. Commit ac8a154 serializes binding lookup, start/resume/end, and
publication with the existing filesystem mutex in a separate per-native-id runtime directory.
The lock awaits the full handler and receives the hook deadline. A corrupt binding is no
longer treated as missing; the existing fail-open error path preserves the session count.
No runtime dependency or adapter capability changed. The storage package exports its
existing mutex for this separate hook sequence; the core storage port is unchanged.

All 49 opening-focused, 47 resume-focused and nine hook-focused checks passed. The final
357 adjacent core/storage/hook/packed checks passed without skips. The new installed tests
cover same-id races, resumed live owners, distinct ids during promotion, metadata and scope,
concurrent hook starts/reattachments, end queued behind first start, expired waiters, corrupt
bindings, and retry after probe failure. Thirteen exact mutations were caught: six opening,
one resume and six hook lifecycle checks. Baseline and restored copies passed.

The independence test initially used a synthetic 1000 ms budget and timed out during broad
concurrent I/O. It now uses the unchanged production default of 5000 ms. The separate expired
waiter case keeps its short deadline, and the mutation that makes unrelated ids share a lock
still fails the independence test.

Packed from a clean committed worktree on Node 26.5.1, npm 11.17.0, macOS arm64. npm ci
installed 14 workspaces; syntax passed for 339 modules. The exact saved archive passed
verify-package: 199 permitted entries, resolved packed documentation links, five certification
manifests, clean executable install, doctor, non-Git coordination with no project writes,
and exact client-home restoration including repeated no-op uninstall.

The same probe scripts on a clean install of that archive observed one successful same-id
CLI attach and five exit-5 conflicts, with its heartbeat usable. All six distinct-id attaches
and heartbeats succeeded. Four executable hook starts left one live session. Scheduled core
races rejected with exit 5 and preserved the winner; wrong-workspace resume returned null.
The previous and current observations refer to their own saved archive bytes.

A full run at ac8a154 completed with 1566 tests: 1563 passed, one failed, two explicit skips.
Its sole failure was the stale CHANGELOG candidate reference, corrected by this metadata
commit. The full suite is rerun after this correction; its outcome is not pre-claimed here.

A failed contender may leave its prepared participant identity without a session; complete
rollback of that preparation is not claimed. An already-running handler may settle after
an outer hook timeout, as before; its lifecycle mutex remains held until settlement. An
out-of-order native end event without a generation cannot identify its predecessor solely
from this lock. These checks do not establish abrupt-crash recovery between session and
binding publication, which is the next separate audit.

No live model client, push, merge, tag or publication was performed. Earlier native captures
retain their original artifact provenance; no certification or capability was promoted.


## Unreleased hook process-crash recovery candidate — 2026-09-07

This later local candidate preserves the preceding release and candidate records above.
It was not pushed, merged, tagged or published.

| Fact | Value |
|---|---|
| Candidate commit | `32146c3b04162409adea9c9dd275a9b3fb026c05` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `264220` |
| Entries | `199` |
| sha256 | `dbc99215ab174b48ce49c9aa49dd3617d812f85703e1b08d57a9d8208bafdecf` |

The preceding installed archive duplicated its live session after its hook was killed
between core session creation and binding publication. A pre-binding alone was insufficient:
a waiter that opened its store before the holder journalled could overwrite that binding
before journal recovery, leaving two live owners after the next retry.

Commit 59968d4 persists the fresh owner pair before session creation and recovers the store
again after lifecycle lock acquisition, before reading its binding. Reserved generations
are insert-only, with checks under both writer locks. Missing or closed owners get fresh
pairs; retries cannot resurrect the old generation. Before-turn projection requires a
successful owner heartbeat. Repeating an end after a committed close clears its binding
without another close event or closing a replacement. SDK identity validation rejects
incomplete bindings rather than treating them as absent.

Commit 32146c3 reports failed hooks through a fixed short stderr diagnostic, preserving
exit 0 and excluding arbitrary payload/error details. It is independently tested and
committed. No runtime dependency, record schema, or adapter capability changed.

Tests use actual installed executables, filesystem writes and SIGKILL of owned fixture
processes. Loader checkpoints schedule the crash and observe lock contention without
simulating successful state writes. They cover solo and durable starts after owner creation
and before final binding rename, a start killed before owner creation, solo/durable close
retries, CLI-closed owners, malformed binding identity, and a waiter already contending
when the journal writer dies.

All 322 adjacent core/hook/SDK/packed checks passed. Twelve isolated source mutations and
one installed-loader recovery mutation were caught; baseline and restored copies passed.
The latter fails on the exact recovered owner pair when post-lock recovery is skipped.
Independent review found no blocking production issue.

npm ci installed all 14 workspaces offline; syntax passed for 344 tracked modules. Packed
from a clean committed worktree on Node 26.5.1, npm 11.17.0, macOS arm64. The exact saved
archive passed verify-package: 199 permitted entries, resolving documentation links,
five certification manifests, executable installation, doctor, a non-Git coordination
cycle with no project writes, and exact reversible client-home restoration.

This evidence covers process termination, including SIGKILL; it does not establish
power-loss durability. No live model session was launched for this stage and no native
capability or certification was promoted. The previously documented limits for a handler
already running at timeout and out-of-order native end events remain unchanged.

A clean install of the exact saved archive retained one original owner after SIGKILL
between session creation and binding publication. The waiter-journal probe retained that
same pair and one live owner; skipping its second recovery reproduced the duplicate,
and restoring recovery passed again. No installed source file was altered.

The final full suite passed with 1583 tests: 1581 passed, zero failed, two explicit skips,
in 158548.483958 ms. Two environment-dependent checks were inapplicable: Gemini CLI is
installed here, and this platform already has a passing capture. No new capability claim
follows from their skips. The candidate-reference gate also
passed after recording this artifact. Final verification introduced no source changes.


## Unreleased hook invocation deadline candidate — 2026-09-07

This later local candidate preserves the preceding release and candidate records above.
It was not pushed, merged, tagged or published.

| Fact | Value |
|---|---|
| Candidate commit | `8ee7a571a5bde2d013dc365c6b44d346e28266e9` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `264720` |
| Entries | `200` |
| sha256 | `72c4ecfc0ed32f537c14fbbc8b2667c89f0f299c35e2dff3cff5ceccf64bbe58` |

On the preceding installed archive a delayed version probe created a live session after
runHook had already returned timedOut. Delayed normalization also remained outside that
budget. Packed regressions reproduced unbounded normalization, context and status work,
late binding publication, and expired replacement/removal. Storage regressions reproduced
late writer acquisition, async updates and journal activation after expiry.

Commit aefcd74 binds the invocation deadline to its filesystem store. Callers may shorten
but cannot extend it. Writer waits and ephemeral operations inherit the bound; atomic
publication checks again after preparing bytes, immediately before rename/link. A prepared
journal cannot activate after expiry. An activated journal and an accepted ephemeral
record still finish their logical publication or recover; cancellation cannot split them.

Commit 8ee7a57 covers the complete hook invocation with one budget and gives its store,
binding operations and probes the same deadline. Late work holds its lifecycle mutex
until settlement but cannot mutate the returned fallback or publish an undecided owner.
Timeouts emit the fixed short diagnostic and still return exit 0. The existing offer-commit
error wording is preserved. Git receives the remaining invocation time; optional version
and process-table probes retain their one-second maximum and safe advisory fallback.

Actual installed acc-hook processes were initially kept alive by owned synthetic git,
claude and ps programs that ignore SIGTERM; each required the fixture's eight-second
watchdog. execFile timeout with SIGKILL now bounds those owned probes. An intermediate
AbortSignal approach failed the Git reproduction and was replaced, not claimed as working.
These fixtures do not invoke a live model client or certify a native capability.

All 462 adjacent core/storage/hook/SDK/CLI/process and packed lifecycle checks passed.
The first adjacent run had one error-wording mismatch; restoring the existing message
made the entire run pass. All 20 exact isolated mutations failed their intended tests;
the baseline and restored copies each passed 25 focused checks. They include the controls
that require accepted records and activated journals to finish after expiry. Independent
review found no blocking production issue. One initial atomic-control fixture used an
old schema constant; it was corrected to the exported current schema before final gates.

npm ci installed all 14 workspaces offline; syntax passed for 349 tracked modules. Packed
from a clean committed worktree on Node 26.5.1, npm 11.17.0, macOS arm64. The exact saved
archive passed verify-package: 200 permitted entries, resolving documentation links,
five certification manifests, executable installation, doctor, a non-Git coordination
cycle with no project writes, and exact reversible client-home restoration.

The deadline fences undecided coordination writes; it is not a hard real-time guarantee
for all operating-system I/O. Temporary files, initial store identity/genesis bookkeeping,
cleanup and completion of already accepted writes may outlive the public timeout. It
does not establish power-loss durability. Out-of-order native end events without a
generation remain a separate limitation. No schema, dependency or capability changed.

A clean install of the exact saved archive returned the delayed probe timeout after
1001.95 ms and retained zero owners after releasing the probe and draining lifecycle work.
Normalization returned before its artificial delay was released. Three additional clean
installs of the same archive ran the TERM-ignoring executable checks: Git completed in
5047.64 ms with exit 0, a diagnostic and no owner; version and ps completed in 1171.36 ms
and 1171.98 ms with one usable advisory owner each and no fabricated version or PID.
None required the watchdog. Installed production source was not edited.

The final full suite passed with 1608 tests: 1606 passed, zero failed, two explicit skips,
in 159225.533959 ms. The same two environment-dependent checks remain inapplicable:
Gemini CLI is installed here, and this platform already has a passing capture. The
candidate-reference gate passed all three checks. Final verification changed no shipped
source. Onboarding/hook trust and an updated live cross-client review are separate next
stages; no new live-model run or capability certification is claimed here.


## Unreleased Codex onboarding candidate — 2026-09-07

This local candidate was not pushed, merged, tagged or published. Earlier records retain
their own artifact provenance.

| Fact | Value |
|---|---|
| Candidate commit | `fabf6e5c779c35be06e30b29f64e02dab529e976` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `265142` |
| Entries | `200` |
| sha256 | `7121e89c21d1d979c84a1f4b8055626cf79ad3415115f634ea5304a64845fdb3` |

The preceding exact installed archive printed installation success while hiding the
adapter's manual activation step and preserved-sandbox access check. Its doctor also
accepted any ACC hook trust substring, including a comment or one old hash, as sufficient
to remove its trust action. A plugin config header was described as enabled even when
the value was false.

Commit 80f9e43 carries optional adapter needsAction through the installer and renders it
in normal CLI output as well as JSON. The Codex adapter names hook review and restart,
and identifies the config path and ACC state directory for a preserved user sandbox.
It asks the user to verify access rather than assume their existing settings are wrong.

Commit fabf6e5 leaves cached hook readiness unverified. It directs current enablement and
trust checks to /plugins and /hooks in Codex. It does not parse or manufacture client
trust, duplicate the client's hash algorithm, or launch App Server during diagnosis.
Onboarding prose and historical trust-preservation claims are corrected accordingly.

Real Codex CLI 0.153.4 hooks/list inspected six isolated temporary-home configurations:
no trust yielded five untrusted hooks; one current hash yielded one trusted and four
untrusted; a commented table yielded five untrusted; stale hashes yielded five modified;
current hashes with a disabled guard yielded five trusted with that guard disabled;
all current enabled hooks yielded five trusted and enabled. This was a metadata query,
not hook execution or a model turn. No user daemon or real user config was changed.

Both packed onboarding regressions failed before the correction; four of five readiness
checks failed while the empty-cache control passed. All 177 adjacent checks then ran:
176 passed, zero failed, one environment-dependent skip. The initial restricted run had
seven local socket EPERM failures; rerunning with the required socket permission passed.
Eight exact source mutations each failed their intended regression; baseline and restored
copies each passed seven focused checks. Independent review found no production blocker.

npm ci installed 14 workspaces offline; syntax passed for 350 tracked modules. The archive
was packed from a clean committed worktree on Node 26.5.1, npm 11.17.0, macOS arm64.
verify-package passed on these exact bytes: 200 permitted entries, five certification
manifests, resolved documentation links, executable clean install, doctor, a non-Git
coordination cycle without project writes, and exact reversible client-home restoration.

The interactive trust UI and a new live model turn are not covered by this audit. No
capability flag, certification version, protocol schema or runtime dependency changed.

A clean install of the exact saved archive repeated all six configurations through real
Codex hooks/list. ACC retained the current-review action for each and never rewrote trust.
Both executable onboarding regressions also passed against separate clean installs of
that same archive, with no repack or installed source edits.

The final full suite passed with 1611 tests: 1609 passed, zero failed, two explicit skips,
in 169205.526875 ms. Gemini CLI is installed here, making the absent-client uninstall
case inapplicable; this platform already has a passing capture, making the uncaptured
platform case inapplicable. The candidate-reference gate passed all three checks.
Final verification changed no shipped source. An updated live Claude/Codex review,
including actual hook activation before work, remains the next separate stage.

## Unreleased foreground review candidate — 2026-09-07

This local candidate was not pushed, merged, tagged or published. Earlier records retain
their own provenance.

| Fact | Value |
|---|---|
| Candidate commit | `4c315f831d7d88f1e00239d9e2e13fe809aeb9a3` |
| Tarball | `agents-can-communicate-0.3.1.tgz` |
| Bytes | `265744` |
| Entries | `200` |
| sha256 | `aa947539f5e0ae4d9491775e3ad809c7a5279f615f6a5769fd468f69c4d37fc2` |

The ordinary delayed-start review used real Claude Code 2.1.263 (`-p`) and Codex CLI
0.153.4 (`exec`) on macOS arm64. Both used native hook-provided owner pairs in a fresh
non-Git normalizeTags fixture. Codex started after Claude's first tool result. Delivery
was off; no prompt supplied owner credentials, no peer message was seeded, and no
operator relayed messages or supplied a continuation prompt. Native `/hooks` review
activated all five current Codex definitions before each pair began. Temporary cache
and trust changes were removed afterward, with the original config restored byte-for-byte.

The onboarding archive (`7121e89…45fdb3`) failed this scenario: Claude started a
background poll and exited before the author's request arrived. ACC retained the queued
request but there was no verdict. Generic foreground-wait guidance passed once
(`d03eedb…2ad41d`), then failed in a fresh run of its revised stop-condition wording
(`ee4485b…7b7f05`). Both failed runs were retained; their waiting authors were deliberately
stopped after the missing verdict was recorded. They are not passing completion captures.

Commit 4c315f8 puts an explicit two-call recipe in all five skills: read inbox, wait
five seconds in the foreground using a separate tool call, then read inbox again.
Every returned message must be consumed. The guidance rejects background watchers and
false promises of resumption, and requires an honest partial handoff if waiting must end.
Troubleshooting explains the same boundary. Runtime code and client background settings
are unchanged. The adapter compatibility notes record the observations and their limits.

Two fresh fixtures installed the exact final archive above. Claude used 13 and 11
standalone short foreground waits, with no explicitly backgrounded tool calls recorded.
In both runs the original native reviewer retrieved the request and sent APPROVE; the
request became acknowledged. The original author retrieved the verdict before its
complete handoff. Both processes exited naturally with code 0, both sessions closed,
and no claims or native owner bindings remained. Independent fixture reruns passed all
eight and six tests, respectively. The repeat's 200 installed files matched both source
and archive byte-for-byte. The captures include self-corrected tool-usage errors; successful
completion is not a claim that every intermediate command succeeded.

The end-to-end verdict check failed on the baseline's absent reviewer reply and passed
both final runs. Existing skill/installed-command checks passed all 13 cases. Removing
the new section from each adapter in turn made the existing heading-consistency test
fail; all five mutations were caught, and baseline/restored copies passed all five
checks. This consistency gate checks distribution, not model compliance. No formal test
file was added. Independent review found no blockers in the final guidance or claims.

npm ci installed 14 workspaces offline and syntax passed for 350 tracked modules. The
first full suite ran 1611 tests with one failure: the candidate-reference test correctly
rejected the old digest after the shipped skills changed. The final archive was then
packed from the clean committed worktree on Node 26.5.1, npm 11.17.0, macOS arm64. Its
SHA-256 is identical to the bytes used by both successful live runs.

These are two observed in-turn polling successes, not a universal guarantee about model
attention. Gemini, Grok and Kimi received the same portable skill guidance but were not
live-tested for waiting. No capability flag, certification version, protocol schema or
runtime dependency changed. ACC does not restart an exited client.

verify-package passed on the exact clean-commit archive: 200 permitted entries, five
certification manifests, resolved documentation links, executable clean install, doctor,
a non-Git coordination cycle without project writes, and exact client-home restoration.
The final full suite passed with 1611 tests: 1609 passed, zero failed, two explicit skips,
in 183094.383125 ms. Gemini CLI is installed here, making the absent-client uninstall
case inapplicable; this platform already has a passing capture, making the uncaptured
platform case inapplicable. The updated candidate-reference gate passed all three checks.
Final verification changed no shipped source.

## Iterative review on the same artifact — 2026-09-07

Repository-only evidence commit `b881fce2c61f1620b20a3720a63fee80cc05556a` retains a two-round review capture.
A clean pack has the same SHA-256 as the preceding foreground-wait archive:
`aa947539f5e0ae4d9491775e3ad809c7a5279f615f6a5769fd468f69c4d37fc2`, 265744 bytes, 200 entries.
No shipped source, skill, runtime test or capability changed in this stage.

Real Claude Code 2.1.263 and Codex CLI 0.153.4 used a fresh non-Git fixture, native owner
pairs, delivery off and stock installed plugins. The author requested review before
editing the deliberately defective normalizeTags implementation. Its two original tests
passed, while an independent specification check failed. Claude reported input mutation,
reordered output, last-spelling retention and missing regression coverage. Codex received
the findings, revised implementation/tests and requested another review.

| Fact | UTC time |
|---|---|
| Initial review request | 18:35:04.658 |
| Reviewer rejection with findings | 18:35:55.165 |
| Author response accepting findings | 18:36:22.315 |
| Revised review request | 18:37:30.686 |
| Reviewer reads revised implementation | 18:37:54.223 |
| Reviewer reads revised tests | 18:37:54.241 |
| Revised implementation approved | 18:38:40.683 |
| Author retrieves approval | 18:38:54.386 |
| Author complete handoff | 18:39:14.888 |

The Read metadata carries the changed implementation hash, matching final disk bytes.
The original sessions completed both rounds; both processes exited naturally with code 0,
both sessions closed and no claims or owner bindings remained. The reviewer acknowledged
the author's handoff. No operator continuation, seeded peer message or manual relay was
used. Native Codex hook review preceded the run; original trust/config bytes were restored
and the temporary cache removed afterward.

Independent fixture reruns passed all nine revised tests and the specification check.
The revised tests against the original implementation failed six cases, demonstrating
that the regression coverage catches the injected fixture defects. Five mutations of
the diagnostic evidence checker were rejected: negative approval, reading only the old
implementation, an unretrieved verdict, premature handoff and missing initial findings.
Baseline and restored records passed. These are checks of this capture and its fixture,
not new ACC runtime regression tests. Independent evidence review found no blockers.

The reviewer found the specification unambiguous, so no clarification exchange occurred.
Both clients shared one checkout; selection of the correct revision across worktrees is
not established. One observed success is not a general model-compliance guarantee.
The full ACC suite was not repeated for repository-only evidence changes; its preceding
result on identical shipped bytes remains 1609 passed, zero failed, two explicit skips.
Syntax passed for 350 modules. This candidate remains local and unpublished.

The clean-commit archive passed verify-package: the permitted file set, documentation
links, executable clean install, non-Git CLI coordination and exact client-home restoration.
The candidate-reference gate passed all three checks after updating the recorded commit;
its tarball digest and bytes are unchanged from the live-tested archive.

## Cross-client continuation through a recorded handoff — 2026-09-07

Repository-only observation commit `4c4034136b574419e8d7161f938a9e5e3348cfbb` records one real sequential
Claude Code 2.1.263 → Codex CLI 0.153.4 continuation on macOS arm64. A clean pack retains
SHA-256 `aa947539f5e0ae4d9491775e3ad809c7a5279f615f6a5769fd468f69c4d37fc2`,
265744 bytes and 200 entries. All 200 clean-installed files matched source and
archive before the native experiment. No shipped source, skill or test changed.

The fresh non-Git stock fixture required parseStockRow and summarizeStock. Claude was
asked to implement only the validation slice, verify it, preserve the remaining product
decision in a partial ACC handoff and stop. The decision was to sum normalized SKU totals
and clamp each total to 17; throwing on overflow had been rejected. The number 17 was
absent from all four project files and the successor prompt. The helper's nine tests
passed, while an independent check failed because aggregation was not yet implemented.

Claude recorded the partial room handoff at 18:54:32.943Z and exited naturally before the
fresh Codex conversation started. Native Codex hook review activated five current ACC
definitions in a separate activation runtime. The successor received no old transcript,
message id or owner pair. Its inbox was empty because it did not exist at send time.
Public sync returned the prior handoff id at 18:57:25.063Z, with implementation bytes
still unchanged. Codex completed the carried ceiling/clamping behavior and recorded its
own complete handoff at 19:00:01.753Z.

The native sessions had different participants and their own hook-provided identities.
The original handoff remained unchanged; public history recovery created no addressed
receipt for the new participant. Both processes exited naturally with code 0, both
sessions closed and no claims or bindings remained. Original Codex config bytes were
restored, and the temporary real plugin cache was removed. No model continuation prompt
or operator relay was used. ACC did not start the successor; the test harness did.

Independent verification passed all 22 final fixture tests, the original nine helper
tests against the final module, and a specification oracle covering the carried decision.
The diagnostic checker rejected overlapping process lifetimes, missing ACC recovery,
completion attributed to the old owner and a fabricated receipt. Changing the fixture's
ceiling from 17 to 18 also failed the oracle. Baseline/restored captures passed; these
five mutations test the evidence and fixture, not new ACC runtime regressions.
Independent review found no blockers in the observation or its stated limits.

This is one explicit saved-handoff continuation, not abrupt-crash recovery, a real quota
exhaustion, or recovery of context the first model never saved. Delivery was off. No
native capability or certification changed. The full suite was not repeated for this
repository-only evidence update; its previous result on the same shipped bytes remains
1609 passed, zero failed and two explicit skips. Syntax passed for 350 tracked modules.
This candidate remains local and unpublished.

The exact clean-commit archive passed verify-package, including permitted entries,
documentation links, executable clean installation, non-Git coordination and exact
client-home restoration. The updated candidate-reference gate passed all three checks.

## Abrupt client interruption after a saved decision — 2026-09-07

A clean pack from `3a061c16e344ee02a2a2670545989c8ea9537d9a` retained the live-tested
SHA-256 `aa947539f5e0ae4d9491775e3ad809c7a5279f615f6a5769fd468f69c4d37fc2`,
265744 bytes and 200 entries. All 200 source/archive/installed files matched before
the experiment. No ACC runtime source, installed skill or formal test changed.

In a fresh non-Git stock fixture, Claude Code 2.1.263 implemented and tested a helper,
renewed two 180-second advisory claims and saved an unaddressed decision. Its content
preserved a per-SKU aggregation ceiling of 23, clamping overflow, the rejected throwing
alternative, completed verification and remaining work. The harness injected SIGKILL
immediately after the successful message result at 19:18:44Z; no finish was recorded.
The original session remained open but was classified offline. Its two claims survived
until 19:21:24.755Z and 19:21:26.525Z.

A fresh Codex CLI 0.153.4 started after that process exited, with its own hook identity.
It received no prior transcript, owner pair, message id or ceiling in its prompt.
Public sync returned the saved decision before implementation changes were observed.
At 19:20:05.490Z its claim returned exit 5 with the exact old claim id, resource,
offline owner and expiry. Explicit instructions allowed a four-minute wait and forbade
force release. Its own claims committed at 19:21:40.379Z and 19:21:40.645Z. Hash sampling
began at 19:21:15Z with unchanged files; the first observed revision was at 19:23:37Z.
Sampling at 250 ms cannot exclude transient changes between samples or earlier.

Codex completed the recovered rule, recorded its own handoff at 19:24:21.851Z and exited
naturally with code 0. The completion tool result and final observation matched delivered
file hashes. No post-crash event used the old owner and no force release occurred.
Active claims and live sessions ended at zero. The old session remained open/offline,
with two expired claim records and one old hook binding; the successor closed and released
its own claims. Its final report's “all claims were released” overstated cleanup: the old
claims expired and their records remained. Completion addressed to the old participant has
a queued receipt; it was not retrieved or acknowledged. The original decision is unchanged.

Independent verification passed 22 final fixture tests, the original nine helper tests,
and a specification oracle. Six capture mutations plus an actual ceiling change 23 → 24
were rejected. Review found seven additional evidence-checker gaps around provenance,
observation completion and final revision ordering: the old checker accepted all seven
tampered records; the corrected checker rejected them, with baseline/restored checks
passing. These fourteen mutations check this capture and fixture, not ACC runtime code.
Independent review found no remaining blockers for these bounded evidence claims.

The number 23 also existed in a predecessor test as an invalid helper input. The numerical
canary therefore failed its absence check; the ceiling/clamping rule was absent from the
specification and predecessor implementation. This is one observed recovery of a saved
decision after process death, with explicitly requested claim waiting. It establishes no
unsaved-context recovery, actual quota exhaustion, automatic client restart, write guard
or new capability. Delivery was off; 180 seconds was an explicit test lease, compared with
the 1800-second default. Native Codex /hooks reviewed five definitions before model runs;
original config/trust bytes were restored and the temporary real cache removed afterward.

The exact clean-commit archive passed verify-package, including permitted contents,
packed Markdown links, executable clean installation, non-Git coordination and exact
client-home restoration. Syntax passed for 350 modules; candidate-reference passed three
checks. The full suite was not repeated for repository-only evidence changes: its previous
result on identical shipped bytes remains 1609 passed, zero failed and two explicit skips.
This candidate remains local and unpublished.
